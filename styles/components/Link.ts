import type { ComponentStyleConfig } from "@chakra-ui/theme";

const Link: ComponentStyleConfig = {
  baseStyle: {
    _focus: {
      boxShadow: "none",
    },
    _hover: {
      textDecoration: "none",
    },
  },
  sizes: {},
  variants: {},
  defaultProps: {},
};

export default Link;
