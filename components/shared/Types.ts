type HeroInfo = {
  imageNumber: number;
  imageUrl: string;
  heading: string;
  paragraph: string;
  buttonText: string;
  discountText: string;
  discountAmount: number;
};

export type { HeroInfo };
