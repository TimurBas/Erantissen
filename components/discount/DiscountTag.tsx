import React from "react";

const DiscountTag = ({
  text,
  amount,
  color,
}: {
  text: string;
  amount: number;
  color: string;
}) => {
  return (
    <div></div>
  );
};

export default DiscountTag;
